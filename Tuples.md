Tuples are a [[data type]] which is immutable (kinda like a [[blockchain]] lol).

E.g:

```python
this_tuple = ("Harry", 7)
```

That can't be changed at all.

So it can be used as dictionary keys, and is apparently also slightly faster than lists.