In [[Linux]] [[Bash]], `.bashrc` is the file in the home dir that lets you customise stuff that happens at startup.

e.g: putting:

```bash
echo "Hello World"
```

in the file will show it all the time upon startup.